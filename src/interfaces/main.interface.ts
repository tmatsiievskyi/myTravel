export enum ActivityType {
  ACCEPT = 'accept',
  ADD = 'add',
  CREATE = 'create',
  READ = 'read',
  UPDATE = 'update',
  DELETE = 'delete',
}

export enum NotificationType {
  PASSWORD = 'password',
  REGISTER = 'register',
  REISSUE = 'reissue',
}

export enum ResourceType {
  AUTH = 'auth',
}
